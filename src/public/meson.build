src_files += files('bigint_c.h')

inc_dirs += include_directories('.')

install_headers(
    files('bigint_c.h', 'bigint_cpp.hpp'),
    install_dir: install_include_dir / 'public',
    preserve_path: true,
)
